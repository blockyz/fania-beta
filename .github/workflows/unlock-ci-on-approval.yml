# .github/workflows/unlock-ci-on-approval.yml
- name: Unlock CI on Approval
  run: gh pr edit ${{ github.event.pull_request.number }} --add-label "ci-unlocked" --repo ${{ github.repository }}
  env:
    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
